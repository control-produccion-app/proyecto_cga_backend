{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "preDeployCommand": "python manage.py ensure_superuser",
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn panaderia_backend.wsgi:application --bind 0.0.0.0:$PORT"
  }
}